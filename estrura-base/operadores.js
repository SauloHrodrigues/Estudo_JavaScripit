/**
 * = : Operador de atribuição, utilizado para atribuir valor a uma variável
 * + : Operador de soma
 * - : Operador de subtração
 * / : Operador de divisão
 * * : Operador de multiplicação
 * ++ : Operador de incremento em 1
 * -- : Operador de decremento em 1
 * += : Incrementar e depois atribuir
 * -= : Decrementar e depois atribuir
 * % : Operador para obter o resto de uma divisão inteira entre dois números
 * && : Operador de E lógico (retorna true se ambas as expressões forem verdadeiras)
 * || : Operador de OU lógico (retorna true se pelo menos uma das expressões for verdadeira)
 * ! : Operador de negação lógica (inverte o valor booleano de uma expressão)
 * ... : Spread operator (expande elementos de um array, objeto ou string)
 * ?? : Operador de coalescência nula (retorna o primeiro operando se não for null ou undefined, caso contrário, retorna o segundo operando)
 */

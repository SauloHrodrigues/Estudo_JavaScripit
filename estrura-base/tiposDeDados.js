/*
 * Como o JS não é uma linguagem tipada, as variáveis passam a ter o tipo de dado inferido a partir do valor que a variável recebe.
*/

const string = "Isso é uma string"
// String são uma sequencia de caracteres delimitados por '' ou ""
const number = 1
// O tipo number são valores numéricos, sejam inteiros ou com ponto flutuante(3.5)
const boolean = false
/*
 * São valores do tipo verdadeiro ou falso.
 * Em um if valores nulos, undefined ou 0 são considerados false. Valores não nulos ou indefinidos e 1 são considerados verdadeiro.
*/
const nulo = null
// Representa que a certeza de não existir nada dentro da variável.
const indefinido = undefined
// Representa que não foi possível definir o que existe dentro da variável.
const objeto = {}
/*
 * Objetos são uma entrutura em js, que contém propriedades
 * Propriedades são estruturas de chave e valor, exemplo: {nome: "Pedro"}
 * No exemplo a chave é nome, e o valor é "Pedro"
*/
const lista = []

/*
 * São uma lista valores'
 * Ex: ["uva", "maçã", "pêra"]
*/


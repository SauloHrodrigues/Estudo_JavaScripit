/* 
    - É uma forma simples de expressar uma condição if-else em uma única linha
    - sintaxe: condicao ? <O que acontece se for verdadeira> : <O que acontece se for falsa>;
*/

const numero = 5

const resultado = numero % 2 === 0 ? "Par" : "Ímpar"

console.log(resultado) // Imprimirá: Ímpar